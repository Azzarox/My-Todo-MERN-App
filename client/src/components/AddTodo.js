import styles from './AddTodo.module.css';
const AddTodo = () => {
    return (
        <a href="#" className={styles['btn-add-todo']}>
            Add Todo
        </a>
    );
};

export default AddTodo;
